[project]
name = "project-hack-supply-chain"
version = "0.1.0"
description = "Python supply-chain analytics dashboard skeleton backed by local CSV data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dash>=2.18.0",
    "pandas>=3.0.2",
    "plotly>=5.24.0",
]
